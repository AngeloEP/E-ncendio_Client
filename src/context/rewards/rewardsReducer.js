import {
    
} from '../../types';

const rewardsReducer = (state, action) => {
    switch (action.type) {
        

        default:
            return state;
    }
}

export default rewardsReducer;